# 交接紀錄

> 這份紀錄由 handoff.command 自動產生，Claude Code 啟動時會自動讀取。

## 狀態

| 項目 | 內容 |
|------|------|
| 時間 | 2026-04-26(深夜延伸) |
| 裝置 | Mac Studio |
| 分支 | main |

## 摘要

延伸補修 Notion 連結 round-trip bug — 原本只存 32 碼 page id,載入時 UI 顯示對不起來,看起來像每次開啟都被改掉。加了 notion_url 欄位保存原 URL + read-only / 修改 toggle + 確認訊息常駐(載入時背景抓 title)。dev/prod 都已切到位,Supabase migration 已執行。

## 上次進度

(2026-04-26 主要工作:見前一筆紀錄。本次補修以下)

- ✅ 加 `projects.notion_url` 欄位(`supabase/migrations/20260427_notion_url.sql`,**已在 Supabase 執行**)
- ✅ 載入時優先讀 notion_url,沒有 fallback 包成 `notion.so/{pageId}`
- ✅ Notion 輸入框 read-only / 編輯狀態 toggle:
  - 已設定 → read-only,「✏️ 修改」按鈕
  - 編輯中 → 可編輯,「測試並儲存」按鈕
  - 儲存成功 → 切回 read-only
  - 清除 → 進入編輯模式讓使用者立刻能貼新 URL
- ✅ 確認訊息常駐:背景去 notion-fetch 抓 title,顯示「已連到 X」
- ✅ dev → prod cp 已完成,index.html 同步

## 注意事項

- **本日已執行兩支 migration**:`20260427_project_status.sql`(三態管理)+ `20260427_notion_url.sql`(URL 保存)
- Notion title 沒存在 DB,每次 page load 會再呼叫一次 notion-fetch,~500ms 延遲使用者幾乎無感
- 舊資料(只有 page id 沒有 url)會 fallback 顯示成 `notion.so/{id}`,點修改重新貼一次完整 URL 就會固定下來
- index-dev.html 跟 index.html 已同步

## 下次從這裡開始

1. 觀察同事使用反饋(Notion / 三態 / 時間欄位 / Whisper 膠囊)
2. Phase 3 主任務:「📬 通知所有與會者」(第 N 次會議時間通知,複用排程機制)
3. ROADMAP.md 的「現場觀察 #1」(project vs meeting 語義)等 Phase 3 開工再決議
4. 若要升級 Whisper 真餘額(方案 B):從 `supabase/functions/openai-usage/index.ts` 開始
