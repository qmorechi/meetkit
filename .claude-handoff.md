# 交接紀錄

> 這份紀錄由 handoff.command 自動產生，Claude Code 啟動時會自動讀取。

## 狀態

| 項目 | 內容 |
|------|------|
| 時間 | 2026-04-26 |
| 裝置 | Mac Studio |
| 分支 | main |

## 摘要

PreMeeting 大幅 UX 改造、Home 清單分頁/過濾/三態管理、開會時間欄位重做、Whisper 本機用量追蹤一條龍完成。dev 改動多次切到正式版,Supabase migration 已執行(projects.status 欄位)。下次可以接 Phase 3 的「📬 通知所有與會者」新功能,或處理同事回饋。

## 上次進度

- ✅ PreMeeting 永遠掛載 + 送出後保留表單
- ✅ 同事預設名單(emmy/debby/leo/...)+ + New 內嵌輸入(localStorage 記常駐)
- ✅ 兩欄已加入/未加入版面,左右互相 shuffle
- ✅ 設定面板攤平(Notion + 與會者直接放表層)
- ✅ 「會議進行」tab 不再自動全螢幕,Header 永遠顯示
- ✅ 開會時間拆成時/分雙獨立輸入框,嚴格 24h + 防中文 IME(onBeforeInput + onCompositionEnd)
- ✅ 修掉時間欄位 stale state bug(自動跳轉時 onBlur 用 relatedTarget 偵測)
- ✅ Home 清單過濾未命名空殼 + 5 個一頁的上下箭頭捲動
- ✅ 專案三態管理(active / paused / closed),按鈕只在 owner 清單
  - DB migration 已執行:`supabase/migrations/20260427_project_status.sql`
- ✅ Whisper 本機用量追蹤(Header「即時同步」旁的 🎙 膠囊)
  - transcribeAllSegments 自動累積 durationSec → localStorage
  - 點膠囊看今日/本月/累計三層,有清空鈕
- ✅ 全部改動推到正式版 `index.html`,目前 dev = prod

## 注意事項

- **Supabase migration 已跑過**,但若部署到新環境要記得跑 `20260427_project_status.sql`
- Whisper 用量是**本機估算**($0.006/分鐘),跨裝置不同步;若要真餘額需開 OpenAI admin key + 寫 `openai-usage` Edge Function
- macOS Safari 對 `type=number` 仍可能被注音 IME 觸發,目前用「攔截 + 清理」雙保險,但無法強制切英數(系統限制)
- index-dev.html 跟 index.html 已同步,下次改動繼續走 dev 流程
- ROADMAP 的「現場觀察 #1」(2026-04-26)— 預約新會議與舊日誌的「project vs meeting」語義衝突,延到 Phase 3 再處理

## 下次從這裡開始

1. 等同事用過新版後,看有沒有反映卡卡的點(時間欄位、暫停/結案、+New、Whisper 膠囊)
2. Phase 3 主任務:「📬 通知所有與會者」(第 N 次會議時間通知,複用排程機制)
3. 若要做 Whisper 真餘額,從 `supabase/functions/openai-usage/index.ts` 開始
4. ROADMAP.md 的現場觀察 #1 要釐清的「project vs meeting」語義,等 Phase 3 開工再決議
