# 交接紀錄

> 這份紀錄由 handoff.command 自動產生，Claude Code 啟動時會自動讀取。

## 狀態

| 項目 | 內容 |
|------|------|
| 時間 | 2026-04-12 18:00 |
| 裝置 | MacBook Air |
| 分支 | main |

## 摘要

完成 QR Code 開啟語音備忘錄功能、修正最近使用專案標題顯示 bug、修正 GitHub Pages .nojekyll 問題。QR code 透過 open-voicememos.html redirect 頁面觸發 voicememos:// URL scheme，button onclick 方式尚待最終確認。

## 上次進度

- ✅ QR code 顯示修正（qrcodejs DOM render）
- ✅ 新增 open-voicememos.html（GitHub Pages redirect → voicememos://）
- ✅ 加入 .nojekyll 修正 index-dev.html 404 問題
- ✅ 修正最近使用專案標題「載入中」bug（code 欄位查詢）
- 🔄 open-voicememos.html button onclick 測試中（window.location 方式）
- ⏭ 專案改名功能（下階段）
- ⏭ Phase 2 音訊 pipeline（ffmpeg.wasm 壓縮 + 切段 + Whisper 上傳）

## 注意事項

- `<a href="voicememos://">` Safari 不認，改用 `onclick="window.location='voicememos://'"` 方式
- 最近使用的 localStorage 存的是 6 碼代碼，查詢必須用 `code=in.(...)` 不是 `id=in.(...)`
- TEST001 在 Supabase 沒有名稱，「未命名專案」是預期行為，改名功能列下階段
- index-dev.html 是開發版，index.html 是正式版，切換需 qmore 明確指示

## 下次從這裡開始

1. 確認 open-voicememos.html button onclick 是否成功開啟語音備忘錄
2. QR code 流程確認後，繼續 Phase 2 音訊 pipeline
3. 測試網址：`https://qmorechi.github.io/meetkit/index-dev.html?p=TEST001`
